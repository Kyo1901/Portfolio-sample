# 🎨 컬러 팔레트 디자인 시스템

## 📝 프로젝트 정보
- **출처 웹사이트**: Apple 공식 웹사이트 (apple.com)
- **분석 날짜**: 2025-11-24
- **적용 프로젝트**: 포트폴리오 웹사이트
- **디자인 컨셉**: 미니멀리즘, 프리미엄 브랜드 이미지

---

## 🎯 CSS 변수 정의

```css
:root {
  /* Primary Colors */
  --color-primary: #0071E3;
  --color-primary-light: #0077ED;
  --color-primary-dark: #0066CC;

  /* Secondary Colors */
  --color-secondary: #1D1D1F;
  --color-accent: #FF9500;

  /* Background Colors */
  --color-bg-primary: #FFFFFF;
  --color-bg-secondary: #F5F5F7;
  --color-bg-tertiary: #FBFBFD;
  --color-bg-dark: #000000;

  /* Text Colors */
  --color-text-primary: #1D1D1F;
  --color-text-secondary: #6E6E73;
  --color-text-muted: #86868B;
  --color-text-on-dark: #FFFFFF;

  /* Border Colors */
  --color-border-light: #E8E8ED;
  --color-border-medium: #D2D2D7;

  /* Interactive Colors */
  --color-button-primary: #0071E3;
  --color-button-hover: #0077ED;
  --color-button-active: #0066CC;
  --color-link: #0071E3;
  --color-link-hover: #0077ED;

  /* Gradient Colors */
  --gradient-sky-start: #4FC3F7;
  --gradient-sky-end: #29B6F6;
  --gradient-orange-start: #FFA726;
  --gradient-orange-end: #FF9500;

  /* Shadow Colors */
  --shadow-sm: rgba(0, 0, 0, 0.08);
  --shadow-md: rgba(0, 0, 0, 0.12);
  --shadow-lg: rgba(0, 0, 0, 0.16);
}
```

---

## 🎨 컬러 사용 가이드

### Primary Color - Apple Blue (`#0071E3`)
**사용 목적**
- 주요 CTA(Call-to-Action) 버튼
- 중요한 링크 및 액션 요소
- 브랜드 정체성 표현

**사용 예시**
```css
/* 기본 버튼 */
.btn-primary {
  background-color: var(--color-primary);
  color: var(--color-text-on-dark);
}

.btn-primary:hover {
  background-color: var(--color-primary-light);
}

.btn-primary:active {
  background-color: var(--color-primary-dark);
}
```

**주의사항**
- 흰색 배경에서 사용 권장
- 접근성을 위해 최소 4.5:1 명도 대비 유지
- 과도한 사용 지양 (1-2개 주요 액션에만 집중)

---

### Secondary Color - Dark Gray (`#1D1D1F`)
**사용 목적**
- 주요 헤딩 텍스트
- 중요한 콘텐츠 텍스트
- 다크 섹션 배경

**사용 예시**
```css
/* 헤딩 텍스트 */
h1, h2, h3 {
  color: var(--color-text-primary);
}

/* 다크 섹션 */
.section-dark {
  background-color: var(--color-secondary);
  color: var(--color-text-on-dark);
}
```

**주의사항**
- 순수 검정색(#000) 대신 사용하여 부드러운 느낌 연출
- 가독성을 위해 충분한 여백 확보

---

### Accent Color - Orange (`#FF9500`)
**사용 목적**
- 특별한 이벤트나 프로모션 강조
- 그라데이션 효과에 활용
- 제한적인 강조 포인트

**사용 예시**
```css
/* 그라데이션 배경 */
.gradient-hero {
  background: linear-gradient(
    180deg,
    var(--gradient-sky-start) 0%,
    var(--gradient-sky-end) 40%,
    var(--gradient-orange-start) 70%,
    var(--gradient-orange-end) 100%
  );
}
```

**주의사항**
- 과도한 사용 지양 (전체 페이지의 5% 이하)
- Primary Blue와 함께 사용 시 신중하게 배치

---

### Background Colors
**레벨별 사용**
- **Primary (White)**: 기본 배경, 콘텐츠 영역
- **Secondary (Light Gray)**: 섹션 구분, 카드 배경
- **Tertiary (Lightest Gray)**: 미세한 구분이 필요한 영역
- **Dark (Black)**: 프리미엄 제품 섹션, 히어로 영역

**사용 예시**
```css
/* 섹션 교차 배치 */
.section:nth-child(odd) {
  background-color: var(--color-bg-primary);
}

.section:nth-child(even) {
  background-color: var(--color-bg-secondary);
}

/* 프리미엄 제품 영역 */
.hero-section {
  background-color: var(--color-bg-dark);
  color: var(--color-text-on-dark);
}
```

---

### Text Colors
**위계별 사용**
- **Primary (`#1D1D1F`)**: 제목, 중요한 본문
- **Secondary (`#6E6E73`)**: 일반 본문, 설명 텍스트
- **Muted (`#86868B`)**: 부가 정보, 캡션, 메타 데이터

**가독성 기준**
```css
/* 최소 명도 대비 비율 */
/* WCAG AA 기준: 4.5:1 (일반 텍스트) */
/* WCAG AA 기준: 3:1 (큰 텍스트) */

.text-primary { color: var(--color-text-primary); } /* 16.04:1 on white */
.text-secondary { color: var(--color-text-secondary); } /* 7.62:1 on white */
.text-muted { color: var(--color-text-muted); } /* 5.24:1 on white */
```

---

## 📱 반응형 고려사항

### 다크 모드 지원
```css
/* 다크 모드 색상 변수 재정의 */
@media (prefers-color-scheme: dark) {
  :root {
    /* Background Colors */
    --color-bg-primary: #000000;
    --color-bg-secondary: #1D1D1F;
    --color-bg-tertiary: #2C2C2E;

    /* Text Colors */
    --color-text-primary: #F5F5F7;
    --color-text-secondary: #A1A1A6;
    --color-text-muted: #6E6E73;

    /* Border Colors */
    --color-border-light: #38383A;
    --color-border-medium: #48484A;

    /* Primary Color (동일 유지) */
    --color-primary: #0A84FF; /* 다크모드 전용 블루 */
  }
}
```

### 모바일 최적화
**터치 타겟 크기**
```css
/* 버튼 최소 크기: 44px × 44px (iOS 기준) */
.btn-mobile {
  min-height: 44px;
  padding: 12px 24px;
  font-size: 16px; /* 모바일 확대 방지 */
}
```

**고대비 모드 지원**
```css
@media (prefers-contrast: high) {
  :root {
    --color-primary: #0051C3; /* 더 진한 블루 */
    --color-text-primary: #000000;
    --color-border-medium: #000000;
  }
}
```

### 색맹 접근성
**색상만으로 정보를 전달하지 않기**
```css
/* 나쁜 예: 색상으로만 구분 */
.status-success { color: green; }
.status-error { color: red; }

/* 좋은 예: 아이콘이나 텍스트 병행 */
.status-success::before {
  content: "✓ ";
  color: green;
}
.status-error::before {
  content: "✗ ";
  color: red;
}
```

---

## 💡 실전 활용 팁

### 1. 컴포넌트별 색상 매핑
```css
/* 버튼 컴포넌트 */
.btn-primary { background: var(--color-primary); }
.btn-secondary {
  background: transparent;
  border: 1px solid var(--color-border-medium);
  color: var(--color-text-primary);
}

/* 카드 컴포넌트 */
.card {
  background: var(--color-bg-primary);
  border: 1px solid var(--color-border-light);
}

.card:hover {
  box-shadow: 0 4px 16px var(--shadow-md);
}
```

### 2. 상태별 색상 변화
```css
/* 인터랙션 상태 */
.interactive-element {
  transition: all 0.3s ease;
}

.interactive-element:hover {
  background-color: var(--color-bg-secondary);
}

.interactive-element:active {
  background-color: var(--color-bg-tertiary);
}

.interactive-element:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}
```

### 3. 애니메이션에서의 색상 활용
```css
/* 그라데이션 애니메이션 */
@keyframes gradient-shift {
  0% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 50%;
  }
}

.animated-gradient {
  background: linear-gradient(
    -45deg,
    var(--gradient-sky-start),
    var(--gradient-sky-end),
    var(--gradient-orange-start),
    var(--gradient-orange-end)
  );
  background-size: 400% 400%;
  animation: gradient-shift 15s ease infinite;
}
```

---

## 📊 색상 조합 추천

### 조합 1: 클린 & 모던
- Background: `#FFFFFF`
- Text: `#1D1D1F`
- Accent: `#0071E3`

### 조합 2: 프리미엄 다크
- Background: `#000000`
- Text: `#F5F5F7`
- Accent: `#0071E3`

### 조합 3: 소프트 & 엘레강트
- Background: `#F5F5F7`
- Text: `#6E6E73`
- Accent: `#FF9500`

---

## ✅ 체크리스트

디자인 시스템 적용 전 확인사항:
- [ ] 모든 색상 변수가 CSS에 정의되었는가?
- [ ] 명도 대비가 WCAG AA 기준을 충족하는가?
- [ ] 다크 모드가 올바르게 작동하는가?
- [ ] 터치 타겟 크기가 충분한가? (최소 44px)
- [ ] 색맹 사용자를 위한 대체 수단이 있는가?
- [ ] 브랜드 일관성이 유지되는가?

---

## 🔗 참고 자료
- [Apple Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)
- [Color Contrast Checker](https://webaim.org/resources/contrastchecker/)
